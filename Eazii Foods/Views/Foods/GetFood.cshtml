@*@model IEnumerable<Eazii_Foods.ViewModel.FoodViewModel>

@{
    ViewData["Title"] = "GetFood";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<section class="section">

    <div class="container p-3">

        <div class="row pt-4 mt-5">

            <div class="col-6 offset-3">

                <h2 class="text-dark btn-block text-center py-2" style="margin-top:150px;">List Of Foods</h2>
                 
            </div>

        </div>

        <br />



        @if (Model.Count() > 0 && Model != null)
        {

                <table class="table-bordered table-striped " style="width:100%">

                    <thead class="btn-block">

                        <tr>

                            <th class="text-center px-5">Name</th>
                            <th class="text-center px-5">Price</th>
                            <th class="text-center">Image</th>
                            <th class="text-center px-5">Date Created</th>
                            <th class="text-center px-5">Action</th>

                        </tr>

                    </thead>

                    <tbody>
                        @foreach (var Food in Model)
                        {
                            <tr>
                                <td width="10%" class="text-center">@Food.Name</td>
                                <td width="5%" class="text-center">@Food.Price</td>
                                <td width="10%" class="text-center">@Food.Image</td>
                                <td width="10%" class="text-center">@Food.DateCreated</td>
                                <td width="5%" class="text-center">

                                    <div class="dropdown dropdown-action">

                                        <a class="icon" href="#" data-bs-toggle="dropdown"><i class="bi bi-three-dots"></i></a>

                                        <div class="dropdown-menu dropdown-menu-right">

                                            <a class="dropdown-item" href="#" data-bs-toggle="modal" data-bs-target="#edit_Food" onclick="FoodToBeEdited('@Food.Id')"><i class="bi bi-pencil-square m-r-5"></i> Edit</a>

                                            <a class="dropdown-item" href="#" data-bs-toggle="modal" data-bs-target="#delete_Food" onclick="FoodToBeDeleted(@Food.Id)"><i class="bi bi-trash-fill m-r-5"></i> Delete</a>

                                        </div>

                                    </div>

                                </td>

                            </tr>
                        }
                    </tbody>

                </table>

        }

    </div>

</section>























<div class="row">

    <div class="col-lg-12">
        <div class="card bg-light shadow-lg">
            @if (Model.Count() > 0 && Model != null)
            {
                <div class="card-body">

                    <table class="table datatable">

                        <thead>
                            <tr>
                                <th scope="col">Name</th>
                                <th scope="col">Price</th>
                                <th scope="col">Image</th>
                                <th scope="col">Date Created</th>
                                <th scope="col">Action</th>
                            </tr>
                        </thead>

                        <tbody>
                            @foreach (var Food in Model)
                            {
                                <tr>
                                    <td>@Food.Name</td>
                                    <td>@Food.Price</td>
                                    <td>@Food.Image</td>
                                    <td>@Food.DateCreated</td>
                                    <td class="text-right">
                                        <div class="dropdown dropdown-action">
                                            <a class="icon" href="#" data-bs-toggle="dropdown"><i class="bi bi-three-dots"></i></a>
                                            <div class="dropdown-menu dropdown-menu-right">
                                                <a class="dropdown-item" href="#" data-bs-toggle="modal" data-bs-target="#edit_Food" onclick="FoodToBeEdited('@Food.Id')"><i class="bi bi-pencil-square m-r-5"></i> Edit</a>
                                                <a class="dropdown-item" href="#" data-bs-toggle="modal" data-bs-target="#delete_Food" onclick="FoodToBeDeleted(@Food.Id)"><i class="bi bi-trash-fill m-r-5"></i> Delete</a>
                                            </div>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>

                    </table>

                </div>
            }
        </div>

    </div>

</div>*@